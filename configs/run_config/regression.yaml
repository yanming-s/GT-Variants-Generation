task: "regression"
pred_target: "logp"
batch_size: 128
num_epoch: 10

lr: 0.001
weight_decay: 0.0
check_val_every_n_epoch: 2

save_model: True
test_only: False
resume: null # absolute path to the model checkpoint

use_gpu: True
gpu_id: 0

wandb_mode: "online"