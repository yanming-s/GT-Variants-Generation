defaults:
    - _self_
    - dataset : zinc
    - task: regression
    - train_and_test: train_default
    - transformer_model: gtv1
    # - generator_model: ddpm


hydra:
  job:
    chdir: True
  run:
    dir: outputs/${now:%Y-%m-%d}/${now:%H-%M-%S}-${dataset.name}-${task.name}